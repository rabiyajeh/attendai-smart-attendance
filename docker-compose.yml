services:
  attendai:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8501:8501"
    volumes:
      - attendance_data:/app/.data
    environment:
      ATTENDAI_DATA_DIR: /app/.data
    restart: unless-stopped
volumes:
  attendance_data: {}
